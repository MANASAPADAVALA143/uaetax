# Backend environment — copy to project root .env or set in Render/hosting
ENVIRONMENT=production
LOCAL_DEV=false

# Production: never set LOCAL_DEV=true — auth bypass is blocked at startup when ENVIRONMENT=production
